{
    "layer": "top",
    "position": "top",
    "height": 30,
    "modules-left": ["sway/workspaces", "sway/mode", "network"],
    "modules-center": ["clock"],
    "modules-right": ["cpu", "memory", "battery", "pulseaudio", "tray", "custom/power"],
    
    "sway/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "format": "{name}: {icon}",
        "format-icons": {
            "1": "1",  // Terminal
            "2": "2",  // Browser
            "3": "3",  // Code
            "4": "4",  // Music/Media
            "5": "5",  // Virtualization
            "default": ""
        }
    },
    "clock": {
        "format": "{:%H:%M}  ",
        "format-alt": "{:%Y-%m-%d}  ",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },
    "cpu": {
        "format": "{usage}% ",
        "tooltip": false
    },
    "memory": {
        "format": "{}% "
    },
    "network": {
        // 关键配置：刷新间隔设置为 1 秒，否则网速显示不准
        "interval": 1,
        
        // WiFi 连接时的显示格式：图标 + 下载速度 + 上传速度
        "format-wifi": "  ⬇{bandwidthDownBytes:>7} ⬆{bandwidthUpBytes:>7}",
        
        // 有线连接时的显示格式
        "format-ethernet": "󰈀 ⬇{bandwidthDownBytes:>7} ⬆{bandwidthUpBytes:>7}",
        
        // 断网时的显示
        "format-disconnected": "⚠ Disconnected",
        
        // 鼠标悬停显示详细信息（IP地址，信号强度等）
        "tooltip-format": "{ifname}: {ipaddr}/{cidr} (Signal: {signalStrength}%)",
        
        // 最小宽度，防止数字跳动导致栏位抖动
        "min-length": 25,
        "max-length": 25
    },
    "pulseaudio": {
        "format": "{volume}% {icon}",
        "format-icons": {
            "default": ["", "", ""]
        },
        "on-click": "pavucontrol"
    },
    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{capacity}% {icon}",
        "format-icons": ["", "", "", "", ""]
    },
    "custom/power": {
    	"format": "⏻ ",
    	"tooltip": false,
    	"on-click": "~/.config/waybar/power_menu.sh"	
    },
    "tray": {
    	"spacing": 10,
	"show-passive-items": true
    }
}
